apiVersion: v1
kind: Service
metadata:
  name: user-dashboard
  labels:
    app.kubernetes.io/name: user-dashboard
    app.kubernetes.io/part-of: my-bank
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: user-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
