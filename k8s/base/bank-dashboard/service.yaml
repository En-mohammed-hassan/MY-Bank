apiVersion: v1
kind: Service
metadata:
  name: bank-dashboard
  labels:
    app.kubernetes.io/name: bank-dashboard
    app.kubernetes.io/part-of: my-bank
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bank-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
