apiVersion: v1
kind: Service
metadata:
  name: c-user
  labels:
    app.kubernetes.io/name: c-user
    app.kubernetes.io/part-of: my-bank
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: c-user
  ports:
    - name: http
      port: 80
      targetPort: http
